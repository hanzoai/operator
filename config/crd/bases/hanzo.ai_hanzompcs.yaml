---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: hanzompcs.hanzo.ai
spec:
  group: hanzo.ai
  names:
    kind: HanzoMPC
    listKind: HanzoMPCList
    plural: hanzompcs
    shortNames:
    - hmpc
    singular: hanzompc
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyNodes
      name: Ready
      type: integer
    - jsonPath: .spec.threshold
      name: Threshold
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          HanzoMPC is the Schema for the hanzompcs API.
          It manages a multi-party computation threshold signing cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HanzoMPCSpec defines the desired state of HanzoMPC.
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: Annotations are additional annotations applied to all
                  managed resources.
                type: object
              apiPort:
                default: 8080
                description: APIPort is the port exposed for API access on pod-0.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              cache:
                description: Cache configures the optional Valkey cache.
                properties:
                  enabled:
                    description: Enabled controls whether the cache is deployed.
                    type: boolean
                  image:
                    default: ghcr.io/hanzoai/kv:8
                    description: Image is the Valkey container image.
                    type: string
                  resources:
                    description: Resources configures CPU and memory for the cache.
                    properties:
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: Limits describes the maximum amount of compute
                          resources allowed.
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: Requests describes the minimum amount of compute
                          resources required.
                        type: object
                    type: object
                type: object
              dashboard:
                description: Dashboard configures the optional MPC dashboard.
                properties:
                  enabled:
                    description: Enabled controls whether the dashboard is deployed.
                    type: boolean
                  image:
                    default: ghcr.io/hanzoai/mpc-dashboard:latest
                    description: Image is the dashboard container image.
                    type: string
                  resources:
                    description: Resources configures CPU and memory for the dashboard.
                    properties:
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: Limits describes the maximum amount of compute
                          resources allowed.
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: Requests describes the minimum amount of compute
                          resources required.
                        type: object
                    type: object
                type: object
              image:
                description: Image defines the MPC node container image.
                properties:
                  pullPolicy:
                    default: IfNotPresent
                    description: PullPolicy defines when to pull the image.
                    enum:
                    - Always
                    - IfNotPresent
                    - Never
                    type: string
                  repository:
                    description: Repository is the container image repository.
                    type: string
                  tag:
                    default: latest
                    description: Tag is the container image tag.
                    type: string
                required:
                - repository
                type: object
              imagePullSecrets:
                description: ImagePullSecrets lists references to secrets for pulling
                  container images.
                items:
                  type: string
                type: array
              ingress:
                description: Ingress configures external access via an Ingress resource.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are additional annotations applied to
                      the Ingress resource.
                    type: object
                  clusterIssuer:
                    default: letsencrypt-prod
                    description: ClusterIssuer is the cert-manager ClusterIssuer to
                      use for TLS certificates.
                    type: string
                  enabled:
                    description: Enabled controls whether an Ingress resource is created.
                    type: boolean
                  hosts:
                    description: Hosts lists the hostnames for the Ingress.
                    items:
                      type: string
                    type: array
                  ingressClassName:
                    default: ingress
                    description: IngressClassName specifies the Ingress class to use.
                    type: string
                  pathRules:
                    description: PathRules defines per-path routing rules.
                    items:
                      description: PathRule defines a single path-based routing rule
                        within an Ingress.
                      properties:
                        path:
                          description: Path is the URL path to match.
                          type: string
                        pathType:
                          description: PathType specifies how the path is matched.
                          enum:
                          - Exact
                          - Prefix
                          - ImplementationSpecific
                          type: string
                        port:
                          description: Port is the backend service port for this path.
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                      required:
                      - path
                      - pathType
                      - port
                      type: object
                    type: array
                  tls:
                    default: true
                    description: TLS enables TLS termination on the Ingress.
                    type: boolean
                  zeroTrustPolicy:
                    description: ZeroTrustPolicy configures zero-trust access control
                      on the Ingress.
                    properties:
                      allowedDomains:
                        description: AllowedDomains lists email domains permitted
                          access.
                        items:
                          type: string
                        type: array
                      allowedEmails:
                        description: AllowedEmails lists email addresses permitted
                          access.
                        items:
                          type: string
                        type: array
                      allowedGroups:
                        description: AllowedGroups lists IAM groups permitted access.
                        items:
                          type: string
                        type: array
                      enabled:
                        description: Enabled controls whether zero-trust policy is
                          enforced.
                        type: boolean
                      iamEndpoint:
                        description: IAMEndpoint is the identity provider endpoint
                          used for verification.
                        type: string
                      sessionDuration:
                        default: 24h
                        description: SessionDuration controls how long a session remains
                          valid.
                        type: string
                    type: object
                type: object
              labels:
                additionalProperties:
                  type: string
                description: Labels are additional labels applied to all managed resources.
                type: object
              p2pPort:
                default: 4000
                description: P2PPort is the port used for inter-node communication.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              replicas:
                description: Replicas is the number of MPC nodes in the cluster.
                format: int32
                minimum: 2
                type: integer
              resources:
                description: Resources configures CPU and memory requests/limits.
                properties:
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Limits describes the maximum amount of compute resources
                      allowed.
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Requests describes the minimum amount of compute
                      resources required.
                    type: object
                type: object
              threshold:
                description: |-
                  Threshold is the minimum number of nodes required for signing.
                  Must satisfy: replicas >= threshold + 1.
                format: int32
                minimum: 1
                type: integer
            required:
            - image
            - replicas
            - threshold
            type: object
          status:
            description: HanzoMPCStatus defines the observed state of HanzoMPC.
            properties:
              conditions:
                description: Conditions represent the latest observations of the MPC
                  cluster's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              keysGenerated:
                description: KeysGenerated indicates whether the MPC key shares have
                  been generated.
                type: boolean
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is the current lifecycle phase.
                enum:
                - Pending
                - Creating
                - Running
                - Degraded
                - Deleting
                type: string
              readyNodes:
                description: ReadyNodes is the number of MPC nodes that are ready.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
