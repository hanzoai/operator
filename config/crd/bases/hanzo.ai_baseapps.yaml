---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: baseapps.hanzo.ai
spec:
  group: hanzo.ai
  names:
    kind: BaseApp
    listKind: BaseAppList
    plural: baseapps
    shortNames:
    - bapp
    singular: baseapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .status.currentWriter
      name: Writer
      type: string
    - jsonPath: .status.term
      name: Term
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BaseApp is the Schema for the baseapps API. It models a
          hanzoai/base-ha cluster: N replicas, one writer pinned by Quasar,
          gateway-integrated via the base_ha upstream.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              BaseAppSpec defines the desired state of a BaseApp — a hanzoai/base-ha
              cluster managed declaratively. The operator emits a StatefulSet, a
              headless service (for pod DNS), a ClusterIP service (for gateway
              round-robin reads), a NetworkPolicy, and (optionally) gateway routing.
            properties:
              consensus:
                default: quasar
                description: Consensus selects the writer-pin protocol.
                enum:
                - quasar
                - pubsub
                type: string
              env:
                description: |-
                  Env is appended to the generated env (BASE_*) so operators can
                  supply secrets or feature flags.
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              envFrom:
                description: EnvFrom is appended to the generated envFrom.
                items:
                  description: EnvFromSource represents the source of a set of ConfigMaps
                    or Secrets
                  properties:
                    configMapRef:
                      description: The ConfigMap to select from
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        optional:
                          description: Specify whether the ConfigMap must be defined
                          type: boolean
                      type: object
                      x-kubernetes-map-type: atomic
                    prefix:
                      description: |-
                        Optional text to prepend to the name of each environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    secretRef:
                      description: The Secret to select from
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        optional:
                          description: Specify whether the Secret must be defined
                          type: boolean
                      type: object
                      x-kubernetes-map-type: atomic
                  type: object
                type: array
              gateway:
                description: |-
                  Gateway configures gateway wiring for this BaseApp. Optional — a
                  BaseApp without gateway config is still reachable via its ClusterIP
                  service directly.
                properties:
                  gatewayName:
                    description: |-
                      GatewayName is the ConfigMap name holding the gateway config. If
                      empty, defaults to "gateway" in the HanzoApp namespace.
                    type: string
                  gatewayNamespace:
                    description: |-
                      GatewayNamespace is the namespace of the gateway ConfigMap. If
                      empty, defaults to the BaseApp namespace.
                    type: string
                  leaderPollInterval:
                    default: 1s
                    description: |-
                      LeaderPollInterval is forwarded to the gateway base_ha upstream.
                      Minimum 100ms; default 1s.
                    type: string
                  readYourWritesTTL:
                    default: 5s
                    description: |-
                      ReadYourWritesTTL is forwarded to the gateway base_ha upstream.
                      Set to "0s" to disable read-your-writes pinning.
                    type: string
                  route:
                    description: |-
                      Route is the URL pattern the gateway exposes (e.g. "/v1/app/foo").
                      When empty, no gateway wiring is emitted.
                    type: string
                type: object
              iamApp:
                description: |-
                  IAMApp identifies the optional Hanzo IAM application that owns
                  authentication for this app. When set, the operator forwards the
                  IAM_APP env var so base-ha's boot-time KMS fetch can resolve secrets.
                type: string
              image:
                description: Image defines the container image for the base-ha binary.
                properties:
                  pullPolicy:
                    default: IfNotPresent
                    description: PullPolicy defines when to pull the image.
                    enum:
                    - Always
                    - IfNotPresent
                    - Never
                    type: string
                  repository:
                    description: Repository is the container image repository.
                    type: string
                  tag:
                    default: latest
                    description: Tag is the container image tag.
                    type: string
                required:
                - repository
                type: object
              imagePullSecrets:
                description: ImagePullSecrets are forwarded to the StatefulSet.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              kmsSecrets:
                description: |-
                  KMSSecrets lists KMS secret references synced into K8s Secrets
                  alongside the StatefulSet. Uses the same kms.hanzo.ai/v1alpha1
                  KMSSecret CRD as HanzoService / HanzoDatastore.
                items:
                  description: |-
                    KMSSecretRef generates a KMSSecret CR under kms.hanzo.ai/v1alpha1
                    to sync secrets from Hanzo KMS into a Kubernetes Secret.
                  properties:
                    credentialsRef:
                      description: CredentialsRef references a Kubernetes Secret containing
                        KMS credentials.
                      properties:
                        name:
                          description: name is unique within a namespace to reference
                            a secret resource.
                          type: string
                        namespace:
                          description: namespace defines the space within which the
                            secret name must be unique.
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    envSlug:
                      description: EnvSlug identifies the KMS environment (e.g. "production",
                        "staging").
                      type: string
                    hostAPI:
                      default: https://kms.hanzo.ai/api
                      description: HostAPI is the KMS API endpoint.
                      type: string
                    managedSecretName:
                      description: ManagedSecretName is the name of the Kubernetes
                        Secret to create/update.
                      type: string
                    projectSlug:
                      description: ProjectSlug identifies the KMS project.
                      type: string
                    resyncInterval:
                      default: 60
                      description: ResyncInterval is the interval in seconds between
                        KMS secret re-syncs.
                      format: int32
                      minimum: 10
                      type: integer
                    secretsPath:
                      description: SecretsPath is the path within the KMS project
                        to read secrets from.
                      type: string
                  required:
                  - credentialsRef
                  - envSlug
                  - managedSecretName
                  - projectSlug
                  - secretsPath
                  type: object
                type: array
              networkPolicy:
                description: |-
                  NetworkPolicy configures intra-cluster ingress rules. When nil,
                  the operator emits a default policy allowing pod-to-pod traffic
                  on the BASE port and the HA replication endpoints, plus gateway
                  ingress.
                properties:
                  allowFrom:
                    description: AllowFrom specifies additional peers allowed to connect.
                    items:
                      description: NetworkPolicyPeer identifies a peer for network
                        policy rules.
                      properties:
                        namespaceSelector:
                          description: NamespaceSelector selects namespaces for the
                            peer.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        podSelector:
                          description: PodSelector selects pods within the peer's
                            namespace.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                    type: array
                  allowIngress:
                    description: AllowIngress permits all inbound traffic when true.
                    type: boolean
                  allowIntraNamespace:
                    default: true
                    description: AllowIntraNamespace permits traffic from pods in
                      the same namespace.
                    type: boolean
                  enabled:
                    default: true
                    description: Enabled controls whether a NetworkPolicy is created.
                    type: boolean
                type: object
              partOf:
                description: |-
                  PartOf identifies the application this BaseApp belongs to
                  (app.kubernetes.io/part-of).
                type: string
              port:
                default: 8090
                description: Port is the HTTP port the base-ha pod listens on. Defaults
                  to 8090.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              replicas:
                default: 3
                description: |-
                  Replicas is the desired StatefulSet size. Writer election is
                  deterministic (lowest-sorted alive pod), so any odd number >= 3
                  tolerates floor(n/2) failures.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: |-
                  Resources configures CPU and memory requests/limits for the
                  base-ha container.
                properties:
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Limits describes the maximum amount of compute resources
                      allowed.
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Requests describes the minimum amount of compute
                      resources required.
                    type: object
                type: object
              schema:
                description: |-
                  Schema names a ConfigMap whose contents are mounted at /app/schema
                  so the base-ha binary can pick up collection migrations on boot.
                  The ConfigMap is expected to contain one Base migration file per
                  key. When empty, the image's built-in schema is used.
                type: string
              serviceAccountName:
                description: |-
                  ServiceAccountName sets the pod identity. Defaults to the
                  namespace-default.
                type: string
              serviceMonitor:
                description: |-
                  ServiceMonitor configures Prometheus scraping of base-ha pod
                  metrics.
                properties:
                  enabled:
                    default: false
                    description: Enabled controls whether a ServiceMonitor is created.
                    type: boolean
                  interval:
                    default: 30s
                    description: Interval defines how frequently Prometheus scrapes
                      the metrics endpoint.
                    type: string
                  metricsPath:
                    default: /metrics
                    description: MetricsPath is the HTTP path to scrape for metrics.
                    type: string
                  metricsPort:
                    default: metrics
                    description: MetricsPort is the named port to scrape metrics from.
                    type: string
                type: object
              storage:
                description: Storage configures the per-pod persistent volume claim
                  template.
                properties:
                  retentionPolicy:
                    allOf:
                    - enum:
                      - Retain
                      - Delete
                    - enum:
                      - Retain
                      - Delete
                    default: Retain
                    description: |-
                      RetentionPolicy determines whether the PVC is retained or deleted
                      when the datastore is removed.
                    type: string
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Size is the requested storage capacity.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    default: do-block-storage
                    description: StorageClassName specifies the Kubernetes StorageClass
                      to use.
                    type: string
                required:
                - size
                type: object
            required:
            - image
            - storage
            type: object
          status:
            description: BaseAppStatus reports observed state.
            properties:
              conditions:
                description: Conditions reports detailed state transitions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentWriter:
                description: |-
                  CurrentWriter is the pod DNS name of the last observed writer.
                  Populated from the gateway's leader poll (when a gateway route
                  is configured) or left blank when unknown.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed.
                format: int64
                type: integer
              phase:
                description: Phase is the current lifecycle phase.
                enum:
                - Pending
                - Creating
                - Running
                - Degraded
                - Deleting
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of pods that have passed
                  readiness.
                format: int32
                type: integer
              term:
                description: |-
                  Term is the monotonic writer-election term observed by the
                  operator. Strictly increasing across failovers.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
